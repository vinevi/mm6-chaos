return {
	title = 'Test',
	chance = 10,
	onBegin = function(self)

			-- for _, monster in Game.Map.Monsters do
			-- 	monster.Fly = true
			-- end
			-- evt.MoveToMap(0,0,0,0,0,0,0,0,'oute2.odm')
		end
}